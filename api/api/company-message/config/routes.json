{
  "routes": [
    {
      "method": "GET",
      "path": "/company-messages",
      "handler": "company-message.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-messages/count",
      "handler": "company-message.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-messages/:id",
      "handler": "company-message.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/company-messages",
      "handler": "company-message.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/company-messages/:id",
      "handler": "company-message.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/company-messages/:id",
      "handler": "company-message.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
